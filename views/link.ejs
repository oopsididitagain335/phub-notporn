<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Link Account</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    .code-display {
      background: #2d2d2d;
      color: #00d1b2;
      padding: 15px;
      border-radius: 8px;
      font-family: 'Courier New', monospace;
      font-size: 1.2em;
      margin: 20px 0;
      text-align: center;
    }
    button {
      background: #007bff;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 6px;
      cursor: pointer;
      margin: 5px;
      font-size: 1em;
    }
    button:hover { background: #0056b3; }
    .footer {
      margin-top: 20px;
      font-size: 0.9em;
      color: #aaa;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🔗 Link Your Discord Account</h1>
    <p>Hello, <strong><%= username %></strong>!</p>

    <ol>
      <li>Join: <a href="<%= inviteUrl %>" target="_blank">discord.gg/MmDs5ees4S</a></li>
      <li>Type in Discord:</li>
    </ol>

    <div class="code-display">
      <code>/link <%= linkCode %></code>
    </div>

    <button onclick="copy()">📋 Copy</button>
    <button onclick="check()">✅ Done</button>

    <div class="footer">
      ⚠️ If you see "JavaScript" error, try incognito.<br>
      Code works only once.
    </div>
  </div>

  <script>
    function copy() {
      navigator.clipboard.writeText('/link <%= linkCode %>')
        .then(() => alert('Copied!'))
        .catch(() => alert('Copy failed.'));
    }
    function check() { window.location.reload(); }
  </script>
</body>
</html>
