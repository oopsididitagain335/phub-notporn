<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PulseHub | For Creators, by Creators</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Inter', sans-serif;
      background: radial-gradient(circle at top, #0f111a, #050508);
      color: #fff;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      line-height: 1.7;
      overflow-x: hidden;
    }
    .container {
      max-width: 900px;
      width: 100%;
      text-align: center;
      padding: 60px 30px;
      background: rgba(20, 22, 35, 0.75);
      border-radius: 20px;
      backdrop-filter: blur(12px);
      border: 1px solid rgba(109, 158, 235, 0.15);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6);
      animation: fadeIn 1s ease-in-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .logo {
      font-size: 4rem;
      margin-bottom: 20px;
      color: #6d9eeb;
      text-shadow: 0 0 20px rgba(109,158,235,0.5);
    }
    h1 {
      font-family: 'Poppins', sans-serif;
      font-size: 3rem;
      font-weight: 800;
      background: linear-gradient(90deg, #6d9eeb, #a084ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 20px;
      letter-spacing: 1px;
    }
    .subtitle {
      font-size: 1.2rem;
      color: #b5b5b5;
      margin-bottom: 30px;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }
    .benefits {
      margin: 30px 0;
      text-align: left;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }
    .benefits li {
      margin-bottom: 16px;
      padding-left: 32px;
      position: relative;
      color: #ddd;
      font-size: 1.05rem;
    }
    .benefits li::before {
      content: "✨";
      position: absolute;
      left: 0;
      top: 0;
      font-size: 1.2rem;
    }
    .cta {
      margin-top: 40px;
    }
    .btn {
      display: inline-block;
      padding: 16px 40px;
      font-size: 1.2rem;
      font-weight: 600;
      color: white;
      background: linear-gradient(90deg, #5865F2, #4c58d7);
      border-radius: 12px;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 8px 20px rgba(88, 101, 242, 0.35);
    }
    .btn:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 28px rgba(88, 101, 242, 0.5);
    }
    footer {
      margin-top: 50px;
      font-size: 0.9rem;
      color: #888;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">🎙️</div>
    <h1>PulseHub</h1>
    <p class="subtitle">A premium community for content creators, streamers, and digital artists. Collaborate. Grow. Thrive.</p>

    <ul class="benefits">
      <li>Network with top creators & collab on projects</li>
      <li>Get early access to tools, templates, and resources</li>
      <li>Exclusive AMAs, feedback sessions, and growth workshops</li>
      <li>Safe space to share ideas, without the noise</li>
    </ul>

    <div class="cta">
      <a href="/signup" class="btn">➕ Join the Waitlist</a>
    </div>
  </div>

  <footer>
    © 2025 PulseHub. Made for creators, by creators.
  </footer>
</body>
</html>
